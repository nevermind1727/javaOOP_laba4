package part2;

public class TractorCrawler extends Tractor {
    public TractorCrawler(String name, int weight, int maxSpeed, int passangersCount, String type) {
        super(name, weight, maxSpeed, passangersCount, type);
    }
}